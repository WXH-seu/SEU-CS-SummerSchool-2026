# 选课模块测试记录（v2）

## 1. 测试环境

| 项目 | 环境 |
| --- | --- |
| 日期 | 2026-09-18 |
| Java | Java SE 1.8 |
| 构建 | Maven 3.6.3（Eclipse 内置） |
| 数据库驱动 | UCanAccess 5.0.1 |
| 数据库 | 临时 Access 数据库 |

## 2. 自动化测试

| 测试 | 覆盖内容 | 结果 |
| --- | --- | --- |
| `CourseMessageSerializationTest` | 教学班 DTO、选课请求的对象流序列化 | 通过，2 项 |
| `CourseServiceIntegrationTest` | v2 建表 / 演示数据、可见性与原因、受众 / 窗口 / 性质 /
  名单 / 角色权限、名额池、并发不超卖、删除原子性与删除并发、受众收紧保护、
  教师名单开放时机 | 通过，23 项 |
| `FullDatabaseBootstrapTest` | 全模块建库、重复启动幂等、旧演示班次迁移与演示选课数据 | 通过，3 项 |
| `NetworkCourseSmokeTest` | Socket 登录、目录查询与课表查询 | 需真实服务器，默认跳过 |

全仓回归（common 30 + server 122 + client 15）：通过，0 失败（3 项网络冒烟默认跳过）。

## 3. 已验证场景

- 学生只看到在读、启用且满足受众规则的教学班；非目标院系或年份区间外的教学班不可见。
- 已选、未到选课窗口、窗口已结束、容量已满、时间冲突均返回明确原因且保持可见。
- 同一学生重复选同一教学班或同一门课的不同教学班被拒绝。
- 两名学生并发抢最后一个名额时，恰好一人成功，选课人数不超过容量（JVM 课程锁生效）。
- 教师只能查询本人教学班名单；管理员可查任意名单；名单不含电话、邮箱。
- 管理员可维护课程 / 教学班 / 受众 / 课程性质；删除仍有选课的教学班被拒绝。
- 课程性质与选课窗口格式校验生效。
- 管理员删除仍有选课的教学班被外键拒绝时整体回滚：教学班、时段、受众和选课记录都保持
  原样，不再留下“教学班还在、排课和受众已被清空”的半成品。
- 并发执行“删除教学班”与“学生选课”时只有一个成功：要么学生选上且教学班完整保留，
  要么教学班被删除且没有遗留选课记录。
- 管理员收紧受众时，如果新规则没有覆盖该班已选学生，保存被拒绝并提示人数与示例学号；
  放宽受众（仍覆盖已选学生）可以正常保存。
- 教师名单在选课窗口结束前不可见（返回 `FORBIDDEN`），窗口结束后可查看；管理员不受限制。
- 两个管理员并发为同一教师新建同一时段的教学班时，只有其中一个能保存成功。

## 4. 兼容性说明

- 检测到旧版选课表（选课记录引用课程编号而非教学班）时，服务端启动自动重建选课
  演示表并重写演示数据；不依赖手工迁移。
- 选课时间与选课时间窗口均采用受控文本，避免 UCanAccess 日期类型差异。
- 已知边界：课程锁为单服务器进程内的 JVM 锁；多进程部署时需改用数据库级原子更新。
